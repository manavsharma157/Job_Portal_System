<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>Job Application Form</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        body {
            background-color: #f8f9fa;
        }

        .application-card {
            max-width: 600px;
            margin: 50px auto;
            border-radius: 12px;
            box-shadow: 0 8px 25px rgb(0 0 0 / 0.1);
            overflow: hidden;
        }

        .card-header {
            background: #0d6efd;
            color: rgb(48, 102, 211);
            font-weight: 600;
            font-size: 1.5rem;
            text-align: center;
            padding: 1.25rem 1rem;
        }

        .form-label {
            font-weight: 600;
            color: #212529;
        }

        .btn-submit {
            background-color: #198754;
            font-size: 1.1rem;
            font-weight: 600;
            padding: 12px 0;
            border-radius: 8px;
            transition: background-color 0.3s ease;
        }

        .btn-submit:hover {
            background-color: #145c32;
        }
    </style>
</head>

<body>
    <%- include('partials/bootstrap') %>
        <%- include('partials/navbar.ejs') %>

            <div class="container">
                <div class="card application-card">
                    <div class="card-header">
                        Apply for This Job
                    </div>
                    <div class="card-body">
                        <form action="/applyJob/<%= userId %>" method="POST" enctype="multipart/form-data" novalidate>
                            <div class="mb-4">
                                <label for="fullName" class="form-label">Full Name <span
                                        class="text-danger">*</span></label>
                                <input type="text" class="form-control form-control-lg" id="fullName" name="fullName"
                                    required placeholder="Your full name" />
                            </div>

                            <div class="mb-4">
                                <label for="email" class="form-label">Email Address <span
                                        class="text-danger">*</span></label>
                                <input type="email" class="form-control form-control-lg" id="email" name="email"
                                    required placeholder="example@example.com" />
                            </div>

                            <div class="mb-4">
                                <label for="phone" class="form-label">Phone Number <span
                                        class="text-danger">*</span></label>
                                <input type="tel" class="form-control form-control-lg" id="phone" name="phone" required
                                    placeholder="+91 98765 43210" />
                            </div>

                            <div class="mb-4">
                                <label for="resume" class="form-label">Upload Resume (PDF) <span
                                        class="text-danger">*</span></label>
                                <input type="file" class="form-control form-control-lg" id="resume" name="resume"
                                    accept=".pdf" required />
                            </div>

                            <div class="mb-4">
                                <label for="coverLetter" class="form-label">Any Message (Optional)</label>
                                <textarea class="form-control form-control-lg" id="coverLetter" name="coverLetter"
                                    rows="4" placeholder="Write a brief message or cover letter..."></textarea>
                            </div>

                            <input type="hidden" name="jobId" value="<%= jobId %>" />

                            <button type="submit" class="btn btn-submit w-100">Submit Application</button>
                        </form>
                    </div>
                </div>
            </div>

</body>

</html>