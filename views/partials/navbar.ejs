<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
    <div class="container">
        <a class="navbar-brand fw-bold fs-3" href="/">JobConnect</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
            <ul class="navbar-nav align-items-center">
                <% if (isLoggedIn) { %>
                    <li class="nav-item">
                        <form action="/logout" method="get" class="d-flex">
                            <button class="btn btn-outline-light fw-semibold px-3" type="submit">Logout</button>
                        </form>
                    </li>
                    <% } else { %>
                        <li class="nav-item me-2">
                            <form action="/signup" method="get" class="d-flex">
                                <button class="btn btn-outline-light fw-semibold px-3" type="submit">Sign Up</button>
                            </form>
                        </li>
                        <li class="nav-item">
                            <form action="/login" method="get" class="d-flex">
                                <button class="btn btn-light text-primary fw-semibold px-3" type="submit">Login</button>
                            </form>
                        </li>
                        <% } %>
            </ul>
        </div>
    </div>
</nav>